import { HideFileDirective } from './hide-file.directive';

describe('HideFileDirective', () => {
  it('should create an instance', () => {
    const directive = new HideFileDirective();
    expect(directive).toBeTruthy();
  });
});
